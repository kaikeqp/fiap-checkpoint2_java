# fiap-checkpoint2_java
identificação da(s) entidade(s), crie uma classe de modelo usando o Java (seguindo o padrão de encapsulamento total) e um programa para testar todos os métodos getters e setters)
